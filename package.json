{
  "name": "my",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "budo src/index.js:bundle.js --ssl --live --dir src",
    "build": "mkdir -p dist && cp src/index.html dist/ && ./node_modules/.bin/browserify src/index.js -t [ babelify --presets [ env ] ] | uglifyjs > dist/bundle.js "
  },
  "author": "kareniel",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-env": "^1.6.0",
    "babelify": "^7.3.0",
    "budo": "^10.0.4",
    "uglify-js": "^3.1.3"
  },
  "dependencies": {
    "choo": "^6.5.1",
    "xhr": "^2.4.0"
  }
}
